<main class="conteudo-pagina produto-detalhe">
    <div class="detalhe-container">
        <div class="img-wrapper">
             <img src="/img/<%= product.image %>" alt="<%= product.name %>">
        </div>
        
        <div class="info-wrapper">
            <h1><%= product.name %></h1>
            <p class="descricao"><%= product.description %></p>
            <p class="preco-grande">R$ <%= product.price %></p>
            
            <a href="/contato" class="btn-comprar">Encomendar via WhatsApp</a>
            <br><br>
            <a href="/produtos" class="btn-voltar">← Voltar para a loja</a>
        </div>
    </div>
</main>